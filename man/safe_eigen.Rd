% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/kron.R
\name{safe_eigen}
\alias{safe_eigen}
\title{Symmetric eigendecomposition with automatic relative jitter}
\usage{
safe_eigen(A, jitter = 1e-06, max_jitter = 0.01, eig_floor = 1e-12)
}
\arguments{
\item{A}{A square symmetric numeric matrix.}

\item{jitter}{Initial relative jitter (default 1e-6).}

\item{max_jitter}{Maximum jitter to try (default 1e-2).}

\item{eig_floor}{Lower bound applied to eigenvalues (default 1e-12).}
}
\value{
A list with `$values` (clamped) and `$vectors`, same shape
  as [base::eigen()].
}
\description{
Mirror of [safe_chol()] for symmetric eigendecomposition. Adds a tiny
relative jitter before calling `eigen(_, symmetric = TRUE)`, retrying
with bigger jitter if the call errors (rare but possible on extreme
inputs). Also clamps the returned eigenvalues at `eig_floor` so that
downstream code dividing by them stays finite.
}
