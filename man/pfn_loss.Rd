% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pfn_train.R
\name{pfn_loss}
\alias{pfn_loss}
\title{Compute the PFN loss for one minibatch}
\usage{
pfn_loss(
  out,
  target_log4,
  target_mu_s,
  target_f,
  weights = list(theta = 1, mu = 1, f = 1)
)
}
\arguments{
\item{out}{List of forward outputs from [nn_pfn()].}

\item{target_log4}{(B, 4) tensor: `cbind(log_theta, log_r)`.}

\item{target_mu_s}{(B, n) tensor.}

\item{target_f}{(B, n, nt) tensor.}

\item{weights}{Optional named list of scalar weights on each loss
component (`theta`, `mu`, `f`). Defaults to all 1.}
}
\value{
A list with `loss` (scalar tensor with grad) and `parts` (named
  numeric vector for logging).
}
\description{
Public so [fit_pfn()] and SBC code can reuse the exact same loss
decomposition during validation. Returns the scalar total loss plus
per-component scalar terms for logging.
}
\details{
`target_log4` is the (B, 4) concatenation of `log_theta` and `log_r`,
which is what the model's `theta_*` head predicts. Pass it through
explicitly so callers control the ordering.
}
