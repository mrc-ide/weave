% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/diagnose.R
\name{diagnose_bayes}
\alias{diagnose_bayes}
\title{Per-parameter diagnostic summary of a `weave_bayes` fit}
\usage{
diagnose_bayes(
  fit,
  rhat_threshold = 1.1,
  ks_pvalue_threshold = 0.05,
  prior_samples = 5000
)
}
\arguments{
\item{fit}{A `weave_bayes` object returned by [fit_bayes()].}

\item{rhat_threshold}{Rhat above which `is_poorly_mixed = TRUE`.
Default 1.10.}

\item{ks_pvalue_threshold}{KS test p-value above which we *cannot*
reject "posterior == prior" and `is_prior_driven = TRUE`. Default
0.05.}

\item{prior_samples}{Number of samples to draw from each prior for the
KS test. Default 5000.}
}
\value{
A data frame with columns `param`, `median`, `q025`, `q975`,
  `rhat`, `ess`, `prior_overlap_p`, `is_poorly_mixed`,
  `is_prior_driven`, `status`.
}
\description{
Returns a data frame with one row per kernel hyperparameter plus `r`,
summarising posterior quantiles, Gelman-Rubin Rhat, effective sample
size, and two automatic flags: `is_poorly_mixed` (chains not merged
on this parameter) and `is_prior_driven` (data did not move the
posterior away from the prior).
}
