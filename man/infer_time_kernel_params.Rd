% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/infer_parameters.R
\name{infer_time_kernel_params}
\alias{infer_time_kernel_params}
\title{Estimate time-kernel (periodic × RBF) parameters from empirical ACF}
\usage{
infer_time_kernel_params(data, period, nt, n, plot = FALSE, max_pairs = 1000)
}
\arguments{
\item{data}{Data frame with column `z_infer` and facetting columns `id`, `t`
(times fastest within site).}

\item{period}{Numeric period used in the periodic kernel.}

\item{nt}{Integer, number of time points.}

\item{n}{Integer, number of sites.}

\item{plot}{Logical; if `TRUE`, show empirical vs fitted correlation–lag curve.}

\item{max_pairs}{Integer; optional downsampling of per-lag pairs for speed.}
}
\value{
A list with `periodic_scale`, `long_term_scale` and `period`.
}
\description{
Forms an across-site time–time correlation matrix of `z_infer` (pairwise
complete), averages super-diagonals by lag to get an empirical ACF, then fits
a product kernel `k(h)=k_per(h; alpha, period) * k_rbf(h; theta)` by
least-squares.
}
