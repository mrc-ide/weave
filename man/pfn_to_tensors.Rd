% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pfn_simulate.R
\name{pfn_to_tensors}
\alias{pfn_to_tensors}
\title{Convert a PFN batch (R arrays) to torch tensors on the requested device}
\usage{
pfn_to_tensors(batch, device = "cpu")
}
\arguments{
\item{batch}{A list returned by [pfn_simulate_batch()].}

\item{device}{Torch device string (default `"cpu"`). Pass `"cuda"` once
you're on a GPU box.}
}
\value{
A list of torch tensors mirroring `batch`'s numeric components.
  Non-tensor metadata (`coords`, `n`, `nt`, `period`, `B`, `priors`) is
  copied through unchanged.
}
\description{
Lazy conversion -- keeps the simulator itself torch-free so it can run
in environments without libtorch (CI, plain R sessions), and lets us
move whole batches to GPU with one call when that becomes relevant.
}
