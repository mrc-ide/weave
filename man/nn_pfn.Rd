% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pfn_model.R
\name{nn_pfn}
\alias{nn_pfn}
\title{Build the PFN torch model for a fixed facility geometry}
\usage{
nn_pfn(
  coords,
  nt,
  period = 52,
  d_model = 64,
  n_heads = 4,
  n_spatial_blocks = 1L,
  kernel_size = 7,
  hidden_channels = 32
)
}
\arguments{
\item{coords}{Data frame with `lat`, `lon`; defines the fixed geometry.}

\item{nt}{Number of timepoints.}

\item{period}{Periodic kernel period (default 52). Used to inform the
temporal kernel size; not strictly required by the architecture but
convenient to keep on the module for inference.}

\item{d_model}{Embedding dimension (default 64).}

\item{n_heads}{Attention heads (default 4); `d_model` must be divisible
by `n_heads`.}

\item{n_spatial_blocks}{Number of stacked spatial attention blocks
(default 1).}

\item{kernel_size}{Time-conv kernel size (default 7).}

\item{hidden_channels}{Conv hidden channel count (default 32).}
}
\value{
An `nn_module` instance. Move to GPU with `model$to(device = "cuda")`.
}
\description{
Returns an `nn_module` whose `$forward(y, mask)` consumes a batched
`(B, n, nt)` count tensor and mask, and returns a list of posterior
parameters:
}
\details{
- `theta_mean`, `theta_logvar` -- (B, 4) Gaussian over
    `log(length_scale, periodic_scale, long_term_scale, r)`
  - `mu_mean`, `mu_logvar`       -- (B, n)
  - `f_mean`, `f_logvar`         -- (B, n, nt)

All Gaussian, all diagonal. Replace the head later for richer families
without touching the encoder/spatial block.
}
