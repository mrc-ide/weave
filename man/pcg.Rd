% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pcg.R
\name{pcg}
\alias{pcg}
\title{Generic preconditioned conjugate gradient solver}
\usage{
pcg(
  b,
  Amv_fun,
  Minv_fun = identity,
  tol = 1e-08,
  maxit = 500,
  verbose = FALSE,
  warn = TRUE
)
}
\arguments{
\item{b}{Right-hand side (numeric vector).}

\item{Amv_fun}{Closure: function(v) returning A v.}

\item{Minv_fun}{Closure: function(v) returning `M^-1 v` (the preconditioner
applied to v). Pass `identity` to fall back to plain CG.}

\item{tol}{Relative residual tolerance, default 1e-8.}

\item{maxit}{Maximum number of iterations, default 500.}

\item{verbose}{If TRUE, prints the residual every 25 iterations.}

\item{warn}{If TRUE (default), `warning()` is emitted when PCG runs out
of iterations or hits non-positive curvature. Pass `warn = FALSE`
when calling from inside an MCMC loop where these conditions are
handled by the caller (eg via a fallback preconditioner) -- otherwise
a long run produces thousands of duplicate warnings.}
}
\value{
A list with `x` (solution), `iters` (iterations used), `converged`
  (logical), `rel_resid` (final relative residual).
}
\description{
Solves A x = b given closures for A v and for the preconditioner `M^-1 v`.
Stops on relative residual or maxit (and only warns when convergence was
not actually achieved at exit).
}
