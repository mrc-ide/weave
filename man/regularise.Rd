% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pcg.R
\name{regularise}
\alias{regularise}
\title{Add a small ridge to a square matrix}
\usage{
regularise(x, lambda = 1e-05)
}
\arguments{
\item{x}{A square numeric matrix.}

\item{lambda}{Non-negative ridge (diagonal) value to add. Default `1e-5`.}
}
\value{
A matrix the same size as `x` with `lambda` added to the diagonal.
}
\description{
In plain terms: this adds a tiny value to the diagonal so the matrix is
better-behaved numerically (e.g., invertible and Cholesky-able).
}
\details{
Technically: returns \eqn{X + \lambda I}, which improves condition number and
ensures positive definiteness when \eqn{\lambda > 0}.
}
