% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pfn_simulate.R
\name{pfn_simulate_one}
\alias{pfn_simulate_one}
\title{Draw one synthetic dataset under the NB-GP generative model}
\usage{
pfn_simulate_one(
  coords,
  nt,
  priors,
  period = 52,
  missingness = list(p_one = 0.2, p_switch = 0.3),
  psi_clamp = 20
)
}
\arguments{
\item{coords}{Fixed-geometry coords data frame (`lat`, `lon`).}

\item{nt}{Number of timepoints.}

\item{priors}{A [pfn_priors()] object.}

\item{period}{Periodic kernel period (default 52).}

\item{missingness}{Named list with `p_one`, `p_switch` for the clustered
missingness pattern.}

\item{psi_clamp}{Symmetric clamp on the log-rate `psi = mu_s + f` to keep
extreme prior draws from producing un-representable counts. Default 20
(i.e. `exp(psi) <= 4.85e8`, well within `.Machine$integer.max`).}
}
\value{
A list with `y`, `mask` (both `n x nt`, integer), `f` (`n x nt`,
  numeric), `mu_s` (length `n`), `log_theta` (length 3), `log_r` (scalar).
}
\description{
Returns an `n x nt` count matrix `y` with a clustered missingness mask,
plus the full ground truth (`f`, `mu_s`, `theta`, `r`) needed for the PFN
supervised loss. Internal helper for [pfn_simulate_batch()].
}
\keyword{internal}
