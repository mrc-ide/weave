% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit.R
\name{fit}
\alias{fit}
\title{Fast point estimator for the spatio-temporal latent field}
\usage{
fit(
  obs_data,
  coordinates = NULL,
  hyperparameters,
  n = NULL,
  nt = NULL,
  period = 52,
  n_var_probes = 30,
  pcg_tol = 1e-06,
  pcg_maxit = 500
)
}
\arguments{
\item{obs_data}{A data frame with columns `id`, `t`, `lat`, `lon`, and a
count column (`y_obs` if present, else `n`). NAs in the count column
denote missing observations.}

\item{coordinates}{**Ignored** -- kept for backward compatibility.
Coordinates are now read from `obs_data`. Will be removed in a future
version.}

\item{hyperparameters}{Either a named list
`list(length_scale, periodic_scale, long_term_scale)`
or (legacy) a length-3 numeric vector in that order.}

\item{n, nt}{**Ignored** -- inferred from `obs_data`. Kept for backward
compatibility.}

\item{period}{Periodic kernel period; default 52 (weekly seasonality).}

\item{n_var_probes}{Number of Rademacher probes used in the Hutchinson
variance estimator. Default 30.}

\item{pcg_tol}{Relative residual tolerance for PCG. Default 1e-6.}

\item{pcg_maxit}{Maximum PCG iterations. Default 500.}
}
\value{
The input `obs_data` augmented with the standard fit columns:
  z_est, tausq, z_min, z_max, lambda_est, lambda_min, lambda_max,
  pred_Q2.5, pred_Q25, data_Q50, pred_Q75, pred_Q97.5, surprisal, etc.
}
\description{
Solves the linearised-Gaussian system via matrix-free PCG with the
Kronecker-eigen preconditioner. Computes a stochastic (Hutchinson)
estimate of the posterior variance for credible ribbons.
}
