% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/kron.R
\name{kron_solve_eigen}
\alias{kron_solve_eigen}
\title{Solve (K + s2 I) x = v using the eigen cache}
\usage{
kron_solve_eigen(v, ke, sigma2 = 0)
}
\arguments{
\item{v}{Numeric vector of length n * nt.}

\item{ke}{Eigen cache from [kron_eigen()].}

\item{sigma2}{Non-negative scalar nugget added to every eigenvalue.}
}
\value{
Numeric vector `x = (K + sigma2 I)^-1 v`.
}
\description{
In the (U_s (x) U_t) basis the system is diagonal with entries
(L_s_i * L_t_j + s2), so the whole solve is a reshape, a divide, and
another reshape. With s2 = 0 this gives `K^-1 v` directly.
}
\details{
Used both as the preconditioner inside PCG and to evaluate the GP
log-density in the slice sampler.
}
