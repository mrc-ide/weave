# Initialise model parameters

Provides starting values for latent parameters based on counts.

## Usage

``` r
data_initial_par(data)
```

## Arguments

- data:

  A data frame containing counts \`n\` and \`observed_mu\`.

## Value

A data frame with an added \`start_par\` column.
