# Pairwise temporal distances

Computes pairwise distances between time points.

## Usage

``` r
get_temporal_distance(times)
```

## Arguments

- times:

  A numeric vector of time indices.

## Value

A symmetric matrix of pairwise temporal distances.
